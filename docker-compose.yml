services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: crm-app
    restart: unless-stopped
    working_dir: /var/www/html
    volumes:
      - ./app:/var/www/html
      - ./php/local.ini:/usr/local/etc/php/conf.d/local.ini
    networks:
      - crm_net
    depends_on:
      db:
        condition: service_healthy

  web:
    image: nginx:alpine
    container_name: crm-web
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./app:/var/www/html
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - crm_net
    depends_on:
      - app

  db:
    image: mariadb:10.11
    container_name: crm-db
    restart: unless-stopped
    environment:
      MARIADB_ROOT_PASSWORD: crm_root_2026
      MARIADB_DATABASE: laravel_crm
      MARIADB_USER: crm_user
      MARIADB_PASSWORD: crm_pass_2026
    volumes:
      - crm_db_data:/var/lib/mysql
    networks:
      - crm_net
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 5

  queue:
    image: laravel-crm-app
    container_name: crm-queue
    restart: unless-stopped
    working_dir: /var/www/html
    command: php artisan queue:work --sleep=3 --tries=3 --timeout=60
    volumes:
      - ./app:/var/www/html
    depends_on:
      db:
        condition: service_healthy
    networks:
      - crm_net
    env_file:
      - ./app/.env

networks:
  crm_net:
    driver: bridge

volumes:
  crm_db_data:
